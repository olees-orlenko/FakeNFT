Филипп Герасимов 

Когорта: 31  
Группа: 4  

Эпик: Статистика  

# Декомпозиция эпика Статистика

---

## Модуль 1: UI / Верстка

#### Экраны и компоненты
- Экран карточки пользователя (аватар, имя, рейтинг, краткая информация)  
  (est: 2.5 часа; fact: 4 часа).

- Создание элемента карточки профиля пользователя  
  (est: 1.5 часа; fact: 2 часа).

- Создание экрана статистики с таблицей пользователей  
  (ячейки со статистикой, рейтингом, именем)  
  (est: 3 часа; fact: 4 часа).

- Создание ячейки таблицы статистики  
  (аватар, имя, рейтинг, позиция в списке)  
  (est: 2 часа; fact: 2.5 часа).

- Создание экрана коллекции NFT пользователя  
  (est: 2.5 часа; fact: 3 часа).

- Создание ячейки коллекции NFT  
  (иконка NFT, название, автор, цена, рейтинг)  
  (est: 2 часа; fact: 3 часа).

- Пустые состояния:
  - Нет пользователей в статистике  
  - Нет NFT у пользователя  
  (est: 1 час; fact: 1.5 часа).

---

## Модуль 2: Логика / Навигация / Состояния

#### Навигация и взаимодействие
- Создание навигации между экранами статистики  
  (переход из списка → карточка пользователя → коллекция NFT)  
  (est: 2 часа; fact: 2.5 часа).

- Добавить переход на сайт пользователя из карточки профиля  
  (через Safari / WebView)  
  (est: 1 час; fact: 1 час).

#### Работа со списками и сортировкой
- Добавить сортировку пользователей по имени  
  (est: 1.5 часа; fact: 1.5 часа).

- Добавить сортировку пользователей по рейтингу  
  (est: 1.5 часа; fact: 1 час).

- Сохранение выбранного типа сортировки  
  (UserDefaults / @AppStorage)  
  (est: 1 час; fact: 0.5 часа).

#### Состояния экрана
- Обработка состояния загрузки для экрана статистики  
  (shimmer / progress view)  
  (est: 1 час; fact: 0.5 часа).

- Обработка состояния ошибки при загрузке данных  
  (alert / placeholder)  
  (est: 1 час; fact: 0.5 часа).

- Обновление UI при изменении сортировки  
  (est: 1 час; fact: 0.5 часа).

---

## Модуль 3: Работа с сетью / Данные

#### Запросы и модели
- Запрос списка пользователей для экрана статистики (GET)  
  и маппинг модели  
  (est: 2 часа; fact: x часов).

- Запрос детальной информации пользователя (GET)  
  и маппинг модели  
  (est: 2 часа; fact: x часов).

- Запрос коллекции NFT пользователя (GET)  
  и маппинг модели  
  (est: 2 часа; fact: x часов).

#### Интеграция с UI
- Интеграция сетевого слоя с экраном статистики  
  (est: 1.5 часа; fact: x часов).

- Интеграция сетевого слоя с карточкой пользователя  
  (est: 1.5 часа; fact: x часов).

- Интеграция сетевого слоя с экраном коллекции NFT  
  (est: 1.5 часа; fact: x часов).

#### Обработка ошибок
- Обработка сетевых ошибок  
  (no internet / server error / empty response)  
  (est: 1.5 часа; fact: x часов).

- Логирование ошибок запросов  
  (est: 1 час; fact: x часов).
